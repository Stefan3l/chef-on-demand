# Porta del server Express
PORT=3000

# Chiave segreta per la firma dei token JWT
JWT_SECRET=la-tua-chiave-segreta-qui

# URL per la connessione al database MySQL (modifica utente, password e nome database)
DATABASE_URL="mysql://nome_utente:password@localhost:3306/nome_database"

# Credenziali per l’invio delle email (es. Gmail SMTP)
EMAIL_USER=il-tuo-indirizzo-email
EMAIL_PASS=la-tua-password-o-app-password

# API Key per Google Maps (usata lato client)
VITE_GOOGLE_MAPS_API_KEY=la-tua-api-key
